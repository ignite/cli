package starportcmd

import (
	"fmt"
	"testing"
)

func Test_isInstalled(t *testing.T) {
	fmt.Printf("test isInstalled: Check given plugins is installed.")
}

func Test_install(t *testing.T) {
	fmt.Printf("test install: Check install process is proprely works.")
}
