package main

import (
	"os"

	"<%= ModulePath %>/app"
	"<%= ModulePath %>/cmd/<%= BinaryNamePrefix %>d/cmd"

	svrcmd "github.com/cosmos/cosmos-sdk/server/cmd"
)

func main() {
	rootCmd, _ := cmd.NewRootCmd()
	// TODO do like gaia  there
  if err := svrcmd.Execute(rootCmd, "", app.DefaultNodeHome); err != nil {
		os.Exit(1)
	}
}
